---
 - name: Start docker service
   service:
      name: "docker"
      state: started
      enabled: yes
   become: true

 - name: Create Nginx Container
   docker_container:
     name: myNginx
     image: nginx:stable
     state: started
     auto_remove: true
     ports: "8080:80"
